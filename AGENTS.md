# 项目工作规范

## 默认运行环境

本项目的默认运行和验证环境是带有 GPU 的 Linux 远程服务器。

## 注意事项

远程服务器会在任务结束或中断后停止。仅存在于服务器或代理会话中、后续任务又无法从仓库推断出的环境准备步骤、启动参数、兼容性结论和故障处理方法，必须整理为应纳入 Git 的代码或文本变更，不能只保留在终端历史、运行日志或代理记忆中。轨迹、截图、模型权重、检查点、下载数据和日志仍属于运行产物，不因本条要求加入 Git。

每次有了一轮代码修改，或者是设计调整，必须执行一次提交和推送。

## 非默认运行环境

项目结构检查、静态检查和使用云端大模型的测试可以在非 GPU 环境运行。Windows 中涉及真实 CraftGround 的运行必须使用 WSL 2。BC、相对优势训练、本地模型推理和模型生成必须在带有 GPU 的 Linux 远程服务器上使用真实模型验证；云端模型相关流程必须使用真实服务验证。

## 当前项目架构

标准输入动作协议 v1 是离线数据、Agent、在线环境、轨迹审核和训练共享的唯一动作合同，不得建立并行的动作领域类型。

| 目录 | 职责 |
| --- | --- |
| `external_dataset_loaders_and_protocol_adapters/` | 加载外部数据集并直接转换为标准输入动作协议 |
| `behavior_cloning_dataset_converters/` | 切分协议适配后的数据并转换为行为克隆课程 |
| `behavior_cloning_training/` | 使用行为克隆课程训练模型 |
| `online_interactive_environments/` | 实现 CraftGround、Godot 等在线环境、动作执行和环境状态管理 |
| `online_environment_interaction_agents/` | 使用云端模型或待训练模型与在线环境交互并生成轨迹 |
| `interaction_trajectory_review_agents/` | 依据环境事实审核单条轨迹的合同、执行状态和任务结果 |
| `relative_advantage_comparison_training/` | 比较同一起点轨迹并执行相对优势训练 |
| `model_judgment_review_agents/` | 复核轨迹比较、排名和选择结论 |
| `environment_validation_tools/` | 编排真实环境验证并生成机器和人工可读报告 |
| `tests/` | 放置与最终职责模块对应的测试代码 |

## 运行产物

所有下载数据、转换数据、轨迹、截图、日志、报告、模型权重和检查点统一放在根目录 `runs/`，不得放入源码模块或 `tests/`。

| 产物 | 本地目录 | Hugging Face 目标 |
| --- | --- | --- |
| 外部数据集 | `runs/external_dataset/<dataset_id>` | 原始仓库 |
| 标准协议适配数据 | `runs/protocol_adapted_external_dataset/<dataset_id>` | `hf_user_name/origin_dataset_name` |
| 行为克隆课程 | `runs/behavior_cloning_dataset/<lesson_name>` | `hf_user_name/lesson_name` |

`dataset_id` 使用 Hugging Face 数据集路径并将 `/` 替换为 `_`。只有任务明确要求发布并提供目标账户、可见性和凭证时才允许上传 Hugging Face。

## 文档

`docs/DATA_INDEX.md` 记录所有数据集的 Hugging Face 路径、本地路径、格式和处理阶段。标准输入动作协议写入 `docs/STANDARD_INPUT_ACTION_PROTOCOL.md`。每个在线环境的用法分别写入 `docs/` 下的独立文档。

## 代码写作风格

本项目采用基于 PEP 8 的现代 Python 工程风格。代码由 Ruff Format 自动格式化，并由 Ruff Check 执行静态规则；公共接口使用中文 Google 风格 docstring，核心合同使用标准偏严格的 Pyright 类型检查。纯逻辑优先实现为无隐藏副作用的函数，固定数据合同优先使用冻结 dataclass，有状态资源和多后端接口使用类与 Protocol。训练和数值代码必须额外说明张量的 shape、dtype、device、取值范围和单位。CLI 只负责配置与依赖装配，业务模块不得在导入时执行环境读取、网络访问或文件写入。模块依赖遵循项目现有职责边界，`shared_tools` 不得反向依赖具体业务模块。

## 测试要求

除非任务明确要求使用 mock 测试，否则所有测试和验收一律不得使用 mock、fake、stub、monkeypatch、伪造模块或伪造外部服务替代真实依赖。缺少真实依赖时必须明确报告未验证，不得通过跳过、替换依赖或缩小断言把测试标记为通过。

纯逻辑测试只能证明被执行的纯逻辑。CraftGround、云端模型、BC、相对优势训练和本地模型生成必须分别在其权威环境中使用真实依赖和真实执行路径验证。GPU 验收必须覆盖任务要求的实际训练或推理流程，不以最小烟雾测试代替。

测试数据必须来自正式数据路径或由测试在临时目录中根据公开合同即时构造。不得在 `tests/` 中保存私有测试数据集、模型生成轨迹、下载数据、截图、模型权重、检查点或其他运行产物。

## 汇报语言

- 使用中文书面语，优先使用直接、明确的肯定陈述。
- 汇报内容应当简短。
- 不得私下为了完成任务偷换执行要求。
- 先报告结论，再给出支撑结论的代码位置、命令结果、运行环境和限制。
- 区分已完成、验证通过、验证失败、未执行和因条件不足无法验证，不使用含混的“应该可用”“基本没问题”等表述。
- 测试结果必须说明验证边界。纯逻辑测试、真实 CraftGround、云端模型和 GPU 训练结果不得相互替代。
- 使用项目既有技术术语；首次出现不常见缩写时说明含义，不创造新的比喻性术语。
- 标题简短并直接说明内容。复杂结论拆成短句、表格或分段说明。
- 不使用说教、夸张、情绪化、营销式或幼稚化表达。
- 仅在澄清边界、比较方案或防止误解时使用对比，不制造与任务无关的对立。
- 区分代码事实、项目合同、用户决策和 Agent 建议，不把建议写成已经确定的项目要求。
- 涉及当前版本、第三方依赖、模型能力或运行环境时，优先依据当前源码、真实运行结果和官方现行资料。
- 因网络、权限、凭证、硬件或依赖不足无法核验时，明确报告限制，不把“无法访问”表述为“不存在”。
