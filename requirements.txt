# ⚠️ 本文件已弃用！
#
# 项目已改为使用 setup.py 的模块化可选依赖。
# 请参考 INSTALL.md 了解详细使用方法。
#
# 推荐方式（按需安装）：
#   pip install -e .             # 仅核心依赖
#   pip install -e .[crafter]    # Crafter 训练
#   pip install -e .[ppo-ad]     # PPO+AD 算法
#   pip install -e .[dreamer]    # DreamerV3
#   pip install -e .[all]        # 全部
#
# 如果必须用 -r requirements.txt 形式安装，请改用：
#   pip install -r requirements/all.txt
#
# ─────────────────────────────────────────────────────────────
# 核心依赖（总是需要）
torch
torchvision
numpy
opencv-python
matplotlib
transformers
wandb
requests
pyyaml

# 条件依赖（按需）—— 见 setup.py extras_require
# - crafter            # 用于 [crafter] / [ppo-ad] / [dreamer]
# - pot                # 用于 [ppo-ad]（最优传输）
# - scikit-optimize    # 用于 [ppo-ad]
# - tensorflow         # 用于 [dreamer]（基线对比）
# - minerl             # 用于 [minecraft]
# - pillow             # 用于 [minecraft]
# - gymnasium          # 用于 [rl]
# - envpool            # 用于 [rl]
# - pytest / black     # 用于 [dev]
