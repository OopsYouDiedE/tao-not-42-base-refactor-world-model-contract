# 教师模型 API。真实 Token 只放在本地 .env 或运行环境 Secret 中。
TEACHER_BACKEND=openai-compatible
TEACHER_API_URL=
TEACHER_API_KEY=
TEACHER_MODEL=
TEACHER_TIMEOUT_SECONDS=240

# 本地 GitHub 和 Hugging Face 分别使用 gh auth 与 hf auth，不在此保存 Token。
HF_USER_NAME=
