# 运行时依赖:只列代码实际 import 的包。
# 2026-06 瘦身:移除 legacy(rhythm/yolo/procgen/mamba 时代)且全仓无 import 的
# tensorflow / tensorflow-datasets / h5py / ultralytics / scipy / mamba-ssm。
torch
torchvision        # utils/visualization.py
numpy
opencv-python      # cv2:VPT 数据集解码 + 可视化
matplotlib         # 训练面板 minecraft_viz / train_probe
transformers       # 视觉骨干 DINOv2/v3(统一走 HF,见 net._load_backbone)+ 任务文本编码 minilm
wandb              # 远程实验记录(--wandb)
requests           # 数据下载(colab 数据准备)
pyyaml             # configs/ 结构 yaml 预设读取(utils/config_io)
crafter            # Crafter 环境(PPO+AD 训练域)

# DINOv3 权重 gated:需 HF token(Colab Secret HF_TOKEN / 仓库根 .env),见 utils/hf_token.py。
# 旧 torch.hub 路径已废弃 ⇒ 不再需要 xformers(DINOv2 SwiGLU 加速);transformers 自带 SDPA。
