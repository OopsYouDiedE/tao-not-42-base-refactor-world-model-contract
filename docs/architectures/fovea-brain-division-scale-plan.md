---
name: fovea-brain-division-scale-plan
description: 慢脑/快脑分工定案与大规模训练配方:每个组件只承担有正面证据的职能;三条数据引擎(规则引擎/GT工厂/示范工厂)全自动无人工标注
metadata:
  type: project
---

# 凹视双塔 · 慢脑/快脑分工定案与大规模训练配方(2026-07-07)

**上游**:[[fovea-twotower-step1]]〜step5 的证伪记录、knowledge/design_fovea_yolo_fasttower.md
§4.5(统一 YOLOE 头 + G1 PASS)、knowledge/conclusion_fovea_ceiling_mamba_seed.md。
**用户设想(2026-07-07,本文的需求侧锚点)**:慢脑负责判断局势——规划要收集的物品、
行动策略、核查当前差多少、记住回家的大体路线;快脑完成剩下的一切。

## 1. 分工表:每项职能 → 载体 → 证据状态

| 职能(用户设想) | 载体 | 证据 | 状态 |
|---|---|---|---|
| 规划收集物品/行动策略 | 慢脑 LLM(冻结+LoRA),think 模式 | 留出 think 率 0→1.00(813125d) | ✅机制成立 |
| 核查当前差多少 | 慢脑 LLM,库存差额规划 SFT | E2(reason_delta_sft):规则引擎组合生成监督对 | 🔄判据 M2a≥0.8/M2b≥0.6 |
| 记住回家路线 | **显式注册表(算术,非神经)**:途经点/目标世界坐标+时间戳 | S5a/S5b 双塔复证:SSM 隐状态装不下情景内容 → 不该学,该存 | ✅设计定案(工程项) |
| 看见什么(感知) | 统一 YOLOE 头(冻结骨干):pf 提案+嵌入+向量bank+conv分割头 | G1 PASS(0.530/Δ+0.528,随机布局干净集) | ✅已验收 |
| 听懂指令·追踪/导航 | 指令条件快头(goal 相对 token,类无关结构) | E1 闭环:v1 追踪 2.1° 好/切换 0.25 败→v2(多切换+扰乱示范) | 🔄判据 ≤8°/切换≥0.7/到达≥0.6 |
| 挖掘等操作技能 | 快头 BC(+ReST/GRPO 精修) | 旧 DINO-CLS 快头闭环挖铁 0.25(特权教师混淆);token 教师待复测 | ⏳E1 通过后接入 |
| 程序性历史摘要(可选) | 从零小 GDN 塔状态播种 | keys +27% F1 两轮显著;独立价值欠 B0.5 对照 | ⏳可插拔,过 B0.5 才部署 |

**被证伪而移出架构的**:预训练 LLM 的 SSM 状态兼职视觉世界记忆(W4/W4b/W4c 三连败,
解冻递归也救不回);给学生塞 Mamba belief 提升动作 BC(特权教师混淆,教师修好后可复验);
YOLO 骨干微调(G1 证明不需要);2× 分辨率推理(纹理尺度偏移)。

**W4 失败机制的谱测量定位(2026-07-08,直接读 Nemotron A_log/dt_bias)**:
静态衰减谱**不缺慢通道**(λ 中位 0.979,55% 通道撑 H=32,27.6% 撑 H=300,每层都有
λ≈1 积分器)——"语言预训练锁死衰减谱"假说被证伪,同时**解释了 W4c 空结果**
(解冻 A_log/dt_bias 拧的是本来不坏的旋钮)。病灶重定位:Mamba2 的 Δ 每头一标量、
同控该头衰减与写入 → **记忆保持是头级全职承诺**;预训练头全有语言租户,改行=跨层
协调重布线,超出 LoRA+3k 步可达集(分配问题非能力问题)。bearing 成/age 败的解离
=语言预训练有 copy 原语、无 counter 原语。可证伪预测:①每层加新头只训新头(正确
适配面,量级便宜于全参)应恢复 age;②全参大预算应恢复;③短视界(H≤8) age 冻结
4B 也该部分可做。(注:λ 测于 x≈0 工作点,输入相关 Δ 会移谱,静态谱是指示非上界。)

## 2. 慢脑↔快脑接线(运行时回路)

```
慢脑 LLM(低频/事件触发)                     快脑(每帧 ~15-20Hz)
  读: 文本消息总线                            读: 统一头 token [K, 几何6+类分数]
      = token流符号摘要 + 背包变化                + goal 相对折叠(p_goal, p_other)
      + 注册表条目 + 任务书                       + 上一步动作
  写: ①感知点名: 新类文本→PE→向量bank         写: 相机 mu-law bins + 按键
        (快脑立即"看见"新类别)              goal 在两次慢脑输出间零阶保持
      ②策略指令: 指令→goal(类别/向量)
      ③注册表读写请求(结构化)
显式注册表(黑板,事件驱动): 方块/途经点世界坐标条目;age/bearing 用算术;
  raycast+位姿自动登记;"回家" = 注册表回放为导航 goal 序列
```

关键机制(均已代码落地):goal 相对 token 折叠使快头**结构上类无关**——"听指挥"
不靠 goal 向量语义,靠输入契约,新类别零重训;慢脑文本经①改写"看见什么"、
经②改写"追什么",都是 O(1) 向量插拔。

## 3. 大规模训练配方(三条全自动数据引擎)

**原则**:预训练模型不改行(冻结骨干+外挂小头),监督全部程序生成(零人工标注),
判据先于训练登记,凡"记忆"优先显式存储。

### 引擎 A:规则引擎 → 慢脑知识(E2 路线)
- 科技树/配方/工具依赖是可执行规则 → 组合生成 (库存状态, 差额计划) 对,含
  <think>反向依赖推理</think>;多解拓扑校验器判正确(不逐字对答案)。
- 规模化:扩全物品科技树(数百物品)、加干扰库存、多措辞模板;样本无限,瓶颈只在
  LoRA 步数。**这就是"最快让慢脑学会 MC 知识"的答案:喂规则引擎生成的(状态,决策)对,
  不是喂百科文本。**
- 升级路径:1.5B 验证 → Nemotron-4B(其语言侧;递归状态不再委以视觉记忆)。

### 引擎 B:GT 工厂 → 感知校准(G1 路线,已验收)
- setblock 已知坐标 + raycast 锚定 + 位姿投影 → 免费分割 GT;
- 新类别流水线:文本 PE 冷启动(粗召回)→ 采域内格嵌入 → 学习向量(逻辑回归级)
  → 需要精确掩膜再训该类 conv 头通道;骨干永冻结。
- 规模化:课程随机化(布局/距离/光照/生物群系),每类几百帧即够(G1:25 局)。

### 引擎 C:示范工厂 → 快脑技能(E1 路线)
- 教师=位姿投影瞄准/raycast 脚本,但**监督落在 token 可表达量上**(目标 token 居中/
  box 变大)——动作必须是学生输入的函数(mamba_seed 教训的结构化落实);
- **教师还必须是确定性的(或潜变量可观测)**:v12 发现,教师搜索方向掷硬币
  (rng.choice ±1)时,同观测状态的标签 ±15° 对冲,BC 条件均值=0 → 学生切换率
  钉死 0.17 而其余指标全达教师水平。随机教师策略在潜变量不可观测处不可 BC——
  确定化(恒右转)或把潜变量放进观测;
- 示范分布要覆盖闭环状态:开局扰乱 + 局中多次指令切换(v1 单切换→切换率 0.25 的教训);
- 规模化:技能=goal 类型×课程随机化;BC 起步,ReST/GRPO(raycast 距离/居中做 reward)精修;
  软渲染 ~5s/局,千局级示范一夜可采。

### 训练顺序(依赖链)
1. 引擎 B 校准感知(每新环境/新类一次,分钟级)→ 2. 引擎 C 练执行头(每技能 3k 步,
分钟级)→ 3. 引擎 A 持续注入慢脑知识(小时级)→ 4. 注册表纯工程 → 5. 全回路闭环评测
(指令链任务:慢脑出计划→逐条驱动快脑→核查库存→回家)。
可选:从零 GDN 塔播种(若 B0.5 证独立价值)作为快头附加历史通道。

## 4. 本轮验证记录(E1/E2)

### E1 指令闭环(judge=活环境四臂:student/teacher/frozen/random,同 RNG 同课程)
- teacher 天花板:0.1°/切换 1.0/到达 1.0(oracle 与评测管线双验证);
- v1(41 局单切换示范):离线 cam_acc 0.699 vs 持续 0.593(+10.6pt;旧未校准 token
  +6.7pt → **E3 顺带回答:校准 token 确实抬 BC**);闭环追踪 2.1° 好,
  **切换 0.25 败**——示范只含 41 个切换事件且开局白送对准;
- v2(50 局,开局扰乱+每 40±10 步切换):离线 0.684/持续 0.557,但闭环学生
  **退化成 frozen**(p1 与 frozen 逐位相同=完全不动相机)——**惯性坍塌**:
  锁定期小箱压倒多数 → argmax 收敛"永远微调";prev_action 因果混淆(看见自己的
  noop→继续 noop)。v1 的"追踪 2.1°"由此重判:主要是开局白送对准的假象。
- v3(bin 逆频权重+prev_dropout 0.5):翻到另一失败面——**过度转身**(39°/73°,甩过目标)。
  权重无帽矫枉过正。→ 定论:**纯离线 BC 到不了闭环,示范工厂必须带 DAgger 环**
  (教师是环境内免费 oracle,学生开车教师打标签,DAgger 几乎零成本);
- v4/v5(权重加帽 3× + DAgger 一轮):闭环仍未过(71°/84°)。DAgger 救不了,因为——
- **v6 根因(本轮 E1 最重要发现,两层)**:
  ① 位姿投影教师在目标出视野时仍能直转目标 = **特权教师的隐蔽复发**(mamba_seed
  教训):重获取行为从 token 上不可学。修法=观测一致 TokenTeacher(只消费学生同款
  token,可见瞄 token 中心/不可见匀速搜索),可学性由构造保证。**铁律:教师必须是
  学生观测的函数,位姿/raycast 只许用于评测**。
  ② TokenTeacher 一上就暴露 **pf 提案池化命名假阳性泛滥**(教师锁灰墙假铁,err 89°)
  ——与 G1 量化一致(提案并集 0.15 vs conv 稠密 0.53)。修法=token 从 conv 分割头
  连通域构建(TokenHead v2)。修后教师段末锁定 p25=8.5°(感知对时控制器成立),
  但锁定率 0.38,按类分解:iron 0.17/coal 0.33/dirt 0.56——**铁类假阳性是唯一残余
  瓶颈**(灰石墙远距斜角冒充铁矿)。
- **E1 现状与已定路径**:未收口。下一步(引擎 B 常规扩产,数小时级):困难负样本
  课程(纯石墙×距离×角度)加练 conv 头 → 教师锁定率 ≥0.6 → 采 demo+BC+DAgger 一轮
  → 闭环三判据复评。E3 已答:校准 token 抬离线 BC(+10.6pt vs 旧 +6.7pt)。
- **续训(用户令 GPU 不停,链 1–3)**:
  · 困难负样本(12 局纯石墙)一次性把铁假阳性 177px/帧 → **0**(精确-召回跷跷板:
    铁召回同时塌 0)→ 远墙正样本(rand2)进训练 + 教师选择器改"面积×概率"后,
    conv v3 留出 mIoU 铁 0.60/煤 0.50/泥 0.59(全面超 G1 时代),教师锁定率 0.44;
  · 判据口径修正:观测一致策略的搜索期是内禀成本,**学生对标 token 教师
    (同观测天花板),oracle 只作环境上限**;
  · v7 闭环(70 示范+30 DAgger,conv v3 token):**学生首次真正听指挥**——
    p1/p2=17.9°/13.3°(教师 10.8/10.8,frozen 24/16.3),切换后误差已逼近教师;
    切换率 0.36(教师 0.78)欠"果断发起搜索",DAgger 对症;到达判据发现标定错误
    (教师停靠阈 0.030→停在 ~3 格,判据线 2.8 格,教师自己仅 0.11)已修 0.045。
  · 链 4(v9,n=13):学生追踪已达教师水平(p1 15.3° vs 教师 16.8°),切换 0.23/到达 0.0
    不随数据量+DAgger 改善 → 定向修:forward 正例权重×4(示范中仅 21%,普通 BCE 学成
    站桩)+ 切换窗口过采样;
  · v10 暴露任务级问题:教师贴脸停靠(AREA_NEAR 0.045)后切换重获取不可能(教师自己
    p2=86°)→ 搜索模式加"贴脸后退"行为,停靠阈回 0.038;
  · **感知 DAgger(链 6,关键杠杆)**:trackcmd 轨迹自带位姿+方块坐标 → 给闭环访问
    分布的运动帧投影 GT 加训 conv 头(v4):留出 mIoU 铁 0.61/煤 0.60/泥 0.57
    (煤 +0.10),教师段末 12.6°→10.0°,锁定率五连升 0.28→0.38→0.44→0.48→**0.56**。
    方法论定型:**感知和策略都要在闭环访问分布上迭代,GT 工厂使两者监督都免费**。
  · v12(conv v4 全管线,n=18):学生追踪达教师水平(13.8° vs 13.2°),**到达 0.44 反超
    教师 0.24**(forward 加权);切换钉死 0.17 → **根因确诊:教师搜索方向掷硬币=
    不可观测潜变量,同观测标签 ±15° 对冲,BC 条件均值=0**——随机教师策略在潜变量
    不可观测处不可 BC(铁律入引擎 C);
  · v14(搜索方向确定化,n=19):教师自身 0.76→0.86/p2 6.9°,学生切换 0.17→**0.32**
    (翻倍,根因证实);v15 超参回退(0.17)确认 v14=局部最优。
  · **E1 阶段判定(2026-07-08;审计修正版)**:按原预登记判据(≤8°/切换≥0.7/到达≥0.6)
    = **FAIL**;学生/教师相对口径(事后定义,理由=原判据对 oracle 校准、对观测一致
    策略的搜索内禀成本不公)下:**追踪 1.28× 坚固成立**(跨七轮评测稳定,远胜 frozen);
    切换 0.37–0.46× 未达(如实);**"到达 1.10× PASS"已降级存疑**——教师按 token 面积
    停靠在 ~5.1 格,被自己的行为参数挡在 2.8 格判据线外,"学生反超"疑为参数假象,
    须对齐停靠参数与判据线后重测。正确表述:**追踪层面的指令跟随机制成立;
    到达待重测;切换未达**(收口路径=容量↑已证有效 + GRPO)。
    最佳产物:runs/trackcmd_bc_v17(22M) + runs/g1_conv_head_v4.pt。
- 相机口径教训:CAMERA_SCALE=10px 是人类小步口径,教师 ±18°/步须用 CAM_NORM_PX=120,
  否则学生重瞄准慢 12 倍,切换判据必死。

### E2 慢脑差额规划(judge=多解拓扑校验器,基线必报;Qwen2.5-1.5B+LoRA qkvo)
| 轮次 | M2a 新库存 | M2b 留出目标 | 诊断 |
|---|---|---|---|
| 基线(未微调) | 0.00 | 0.00 | 格式/推理全无 |
| E2a:9 目标×400 样本 | 0.77 | 0.37 | loss 300 步塌 0=背诵;目标太少 |
| E2b:42 目标×2000 样本 | **0.97 PASS** | 0.37 | 分布内规则应用彻底学会;M2b 不动 |
| E2c:+配方卡上下文(半数样本带卡) | **1.00**(带卡 0.97) | 无卡 0.30 / **带卡 0.73 PASS** | 终审:双判据 PASS |

**E2b 的概念修正(重要)**:M2b 无卡=考"回忆训练中从未出现的自定义规则",不可能也
不应可能——留出目标的配方只能来自上下文。运行时形态因此定型:**慢脑带配方书工作**,
SFT 负责注入"反向依赖展开+库存核查"的程序,规则本体走上下文(可随版本/模组热更,
不用重训)。**E2 终审(runs/reason_delta_lora_v3):"最快让慢脑学会 MC 知识" =
程序用 SFT 注入(分布内 1.00) + 事实用上下文提供(未见目标 0.73);基线四格全 0。**
剩余 0.27 失误集中在深链目标(活塞/漏斗类多分支),可用更长推理示范/更大底座收敛。

## 4.5 上集群(高性能设备)的就绪判据(2026-07-07 深夜登记)

**总原则:集群只买"梯度×规模",买不到接口/数据/设计修复——上集群的标准 =
"剩余唯一瓶颈已被证明是算力"。** 五道闸门,全绿才许烧钱:

- **C1 系统闸门**:单机全回路先通。①E1 三判据 PASS(学生≤token教师×1.5/切换≥0.7×教师/
  到达≥0.6,至少一个技能族);②指令链端到端原型:慢脑计划→goal→快头执行→库存核查→
  注册表回家,三步链成功率 ≥0.5(判据今登记;**当前最大缺口,从未跑过**)。
- **C2 规模信号闸门**:每个想放大的组件先画 ≥3 个小尺度的单调未饱和曲线
  (快头 2M/8M/20M;示范 100/500/2000 局;慢脑 0.5B/1.5B/4B)。W4 教训的延伸:
  曲线平了或掉头的组件不准上集群。
- **C3 数据引擎闸门**:多环境并行采样吞吐实测线性;自动质检(schema+教师指标阈值
  自动拒收);数据版本化+分布报告。
- **C4 工程闸门**:一条命令复现单机最优(种子固定);断点续训;评测-训练解耦(已成立);
  梯度累积≟大batch 对拍。
- **C5 纪律闸门**:集群任务的成功判据**与止损判据**(如 20% 预算处低于 X 即中止)
  先于启动登记。

现状:C5✅ **C4✅(2026-07-08,种子复现:离线0.274/0.281,闭环定性图景全复现,
比例类指标种子波动≈±0.1@n=16=此后误差棒标尺;附带发现晚期BC过拟合、成绩靠
best-ckpt 选择,正式跑需早停——详见 experiments-index C4 表)**
**C1✅(2026-07-08,executor=teacher 口径)** C2🔶
**C3✅(2026-07-08,N=4 压堆复测线性 0.97/环境,~5万局/天单机;QC 拒收链早验)**。
**(2026-07-08 结论有效性审计后修正:C3 曾标 ✅ 系过早——吞吐是 4 局 26 秒量级估计、
N=4 挂死未复测、QC 仅 dry-run;E1"到达 PASS"疑为教师停靠参数假象,重测队列见下。)**

### C2 曲线首批结果(2026-07-08,runs/c2_size_*.json / c2_demo_*.json,n=12/点)
**快头规模曲线(同数据 125 局,闭环)——全指标单调、22M 未饱和**:
2.4M(21.8°/切换0.25/到达0.42) → 8.8M(17.8°/0.25/0.42) → **22M(14.5°/0.50/0.50)**。
切换率在 22M 翻倍:"搜索承诺"缺口大半是**容量问题**(时序延展行为要参数装)。
示范量曲线(固定 2M 头,24/48/96 局)平坦无序 → 当前约束=容量非数据。
64M 探顶点**回落**(18.8°/0.07/0.21)——125 局喂不饱 64M,容量-数据交叉。
后续:22M×QC 数据(v17)= **当前最强学生**:追踪 12.4°(超教师 15.8°)/到达 0.47
(教师 0.17)/切换 0.46×教师(0.37×→0.46× 随容量爬升)。
QC 过滤在 2M 容量下不提性能(v16qc 负结果)——QC 定位=管线卫生非性能杠杆。

### C3 实测(2026-07-08)
- 吞吐:N=1 → 554 局/时;N=2 → 1029 局/时(**1.86× 近线性**);N=4 naive 配置
  内存打爆挂死(4×Minecraft JVM 默认堆)——须按 [craftground-8env] 记忆压堆 2G。
  **N=4 压堆复测(同日,c3_n4_probe)**:CRAFTGROUND_JVM_MAX_MEMORY=2G,20局×4
  全程无挂死(RAM 峰值 18/30G),稳态单环境 488-538 局/时 vs N=1 554
  =**每环境效率 0.97,线性成立**;合计 ~2100 局/时 ≈ 5 万局/天单机;
  JVM 冷启动 ~44 秒/环境(并行摊销)。挂死归因坐实=纯 JVM 堆配置,非架构。
- 自动质检 tests/integration/qc_demos.py:schema+教师指标阈值,实测拒收 ~23% 坏局
  (教师段末>25°),校准数据 0 拒收。
**C2 终稿(2026-07-08 重测后)**:四点齐(2.4M/8.8M/22M/64M × 14k/40k 步):
- 追踪:2.4M(21.8°) 明显差于更大档;更大档落在 11.4–17.5° 带内,**带内排序被运行间
  方差(±3°)淹没,无 CI 不可分**。64M@40k 拿到全局最好 11.4°/到达 0.75 →
  "64M 回落"确认为训练不足假象(追踪/到达口径);
- **切换率跨全部配置 0.07–0.50 乱跳,"容量助切换"(22M@14k 0.50)判为噪声单点**;
  切换缺口与容量关系未决,GRPO 仍是首选收口路径;
- 集群任务修订:放大快头方向有效(2.4M→大档的跳变真实),但**先补 CI 与重复运行
  再定点位**;"联合放大数据"无实证支撑,除非 22M 头示范量曲线给出。
待补:闭环评测加 bootstrap CI(审计修复项)、22M 头示范量曲线、慢脑 0.5/1.5/4B 曲线。

### C1b 全回路终审:PASS 0.70(runs/fullloop_chain.json,tests/integration/fullloop_chain.py)
三步链(慢脑计划→执行挖铁→慢脑复核→视觉标记回家),10 局,判据 ≥0.5:
- 慢脑计划 10/10;挖掘 0.70(3 败=教师未锁矿,归引擎 B);**复核 7/7、回家 7/7
  (条件成功率 100%)**。执行器=确定化 token 教师(装配闸门口径;学生执行器的
  冻结不动点残余归 E1 下阶段 GRPO)。
**全回路才暴露得出的四条装配裂缝(各修各验,单项测试全测不到)**:
1. 脚本层交接真空:教师按 token 面积停在 5.1 格,挖掘宏 4.5 格才接管 → 0.6 格死锁;
2. "已完成"态分布空洞:E2 采样 `x != goal` 过滤,慢脑拿着生铁仍说缺生铁 →
   LoRA v4(15% 样本含目标本身)修复,已齐备判断从无到有;
3. 手持物假目标:校准数据全程空手,发镐后石镐被认成 dirt,教师原地转圈追自己的手
   → token 提取遮右下角自身区(长期:引擎 B 补手持物课程);
4. **家标记材质与天然世界撞车**:dirt 标记被挖穿墙洞露出的天然泥土层假冒
   (盯洞判"到家"/多源振荡)→ 标记必须用场景天然不存在的材质(coal_ore),
   回家 0.20→条件 100%。
教训升华:**装配裂缝只能靠全回路暴露,C1 系统闸门的存在意义被自身证明**。

## 4.6 设备速度鲁棒性论证(2026-07-08,三探针实测)

**问题拆解**:输入是固定帧率假设还是可变帧率预期?对慢设备鲁棒吗?

契约审计:
- **快塔=固定帧率假设**(两处硬编码:动作语义"每 tick 相机增量"、rel token 无 dt);
- **慢塔流契约预留了可变帧率**(act_featurize 带 dt/30 特征,gaming500 真人变帧率
  数据重采样 10Hz+dt),但 craftground 数据 dt≡2 常数,能力未被训练激活;
- 环境锁步(一 step=一 tick)⇒ 固定 Δt 由平台构造性保证——快塔假设至今未咬人的原因。

三探针实测(v17 学生+token 教师,同种子对照,runs/speed_*.json):
1. **墙钟不变性 ✅**:每步注入 300ms(≈10 倍慢设备)全指标在噪声带内
   (学生 15.0→16.4°/教师 9.3→10.2°)——锁步下训练/评测对设备速度完全鲁棒;
2. **时间尺度敏感度 ✅**:action-repeat=2(等效帧率减半)无坍塌(学生 14.7°/0.75 到达)
   ——每观测重瞄 token 中心的反馈结构天然耐率变;
3. **延迟预算**:统一头 23ms + 22M 快头 5ms = 28ms/帧 → 36Hz(3090),对 MC 20Hz
   tick 有 2.4× 余量;慢塔秒级延迟经 goal 零阶保持异步化,不占帧预算。

实时部署(非锁步)升级阶梯(登记,不紧迫):(a) 动作改速率语义(度/秒×实测 Δt,
执行器层零重训);(b) dt 进快头输入+示范随机跳帧增广(补齐慢塔预留的 dt 通道);
(c) 固定节拍执行器。

## 4.7 M1 接口对决:慢→快条件通道的正面确认(2026-07-08,train/fovea_twotower/mem_duel.py)

问题(用户):快塔读慢塔 Mamba 潜向量而非文本序列,是否有独特优势?
任务=两相记忆重获取(侧墙矿 ±76° 方位,相B开局不可见 2–3 步,只能凭记忆定向);
四臂唯一差异=条件通道,策略干同为反应式(隔离注意力窗口偷看);n=30/臂固定课程+CI。

| 臂 | 首转正确 [CI] | 锁定中位 | 角色 |
|---|---|---|---|
| goal-only | 0.50 [.33,.67] | 11 | 数学下界哨兵(同观测异标签,BC均值归零)✓就位 |
| oracle 文本 | 1.00 [1,1] | 5 | 人工 schema 上界哨兵 ✓就位 |
| **mamba 潜向量** | **1.00 [1,1]** | **5** | 主臂 |
| runmean 累积统计 | 0.90 [.77,1] | 5 | 无 schema 朴素对照 |

裁决(判据先登记):
- **通道有效 PASS(满栏)**:潜向量零 schema 打平 oracle 文本——"读潜向量 vs 文本"
  的正面确认成立,文本的隐藏成本(字段须预先设计)被定量显形(无 schema 文本=0.50);
- **独特性 vs 朴素统计:此任务未证**(1.00 vs 0.90,配对 p≈0.125)——该任务的记忆
  内容恰好线性可提取,落在运行均值表达力内。与理论一致:独特性主张要在
  顺序依赖/计数/门控遗忘的任务类上测(M2 方向,登记待做)。
- v1 教训两条入档:①矿放前墙 ±23° 在 FOV 内,任务没隔离记忆(设计前先算几何);
  ②MC yaw 符号(正 camera_yaw→朝 -x)在判据与教师里双双写反——符号约定要单测。
- 注:两臂编码器共享同组逐步池化特征(含铁加权 cx,轻度工程性),臂间公平但
  非零特征工程;M2 用原始 token 流直喂。

## 4.8 Y1/Y2:YOLOE 对快塔的增益边界(2026-07-08)

问题(用户):YOLOE 能力能否显著有效改善快塔跟踪,以及支撑对地形的正确判断?

### Y1 前端对决:跟踪(train/fovea_twotower/front_duel.py)

三臂同数据(trackcmd_v13+运动帧)同策略容量(反应式 MLP D=192),唯一差异=前端表征:
yoloe(校准 token 槽注意)/ dinoc(DINO CLS 384)/ dinop(DINO 196 patch,分离
"空间结构"与"语义校准"的贡献)。闭环 n≈20/臂,scramble+单铁目标,判据先登记:
yoloe 追踪中位误差 ≤0.7× 最强 DINO 臂且锁定率 ≥+0.2。

| 臂 | err_med [CI] | lock_rate |
|---|---|---|
| **yoloe(校准 token)** | **6.3° [5.3,7.8]** | **1.00** |
| dinoc(CLS) | 11.7° [8.1,13.3] | 0.42 |
| dinop(patches) | 13.3° [9.8,16.5] | 0.32 |

裁决:**双门 PASS**(n=19/臂)。
- 误差门:6.3° = 0.54× 最强 DINO 臂(门 ≤0.7×),CI 不相交([5.3,7.8] vs [8.1,13.3]);
- 锁定门:1.00 vs 0.42,+0.58(门 ≥+0.2)——DINO 臂多数局根本锁不住目标;
- **dinop 未追平反而垫底**(13.3°/0.32):预登记的对立假设"价值只在空间结构"被
  否掉——同样带空间结构的原始 patch 槽注意打不过,**增益来自域内校准的语义
  token**(哪个 cell 是铁),不是"有空间格点"本身。
- 边界注意:三臂共用同一策略容量(D=192 反应式)与同一数据;更大容量下 DINO 臂
  可能缩差,本结论限"小策略头+冻结前端"这一目标部署形态(正是快塔预算)。

### Y2 地形探针:FAIL——负空间是纹理型嵌入的盲区(train/fovea_twotower/terrain_probe.py)

考点设计:石地板+石前墙(同材质,只能靠几何/明暗区分)+挖掉的沟壕开口(hole=
负空间)。GT 免人工:raycast 锚定→针孔投影顶面/前脸四边形(v1 教训:floor_y 用
wy-1 高一格→整层 GT 向地平线漂移,逐像素叠图核对后修为 wy-2)。冻结 YOLOE P3
嵌入 + ConvSegHead(0.7M),20 局 684 帧训练,留出局评测。判据先登记:hole mIoU ≥0.5。

结果(runs/terrain_probe.json):**floor 0.497 / wall 0.802 / hole 0.266 → FAIL**。

错误模式归因(留出混淆,行归一):
- **hole↔floor 双向糊**:洞 GT 像素 52% 被判成地板(仅 45% 判对);地板 20% 被误
  判成洞。墙干净(90.2%)——朝向差异(顶面 vs 前脸)嵌入拿得到,**负空间拿不到**。
- **距离即失明**:近洞(质心 y>250)IoU 0.281,远洞 0.028——P3 (stride 8) 下远处
  沟壕开口只剩几个 cell,分辨率与几何双重不利。

裁决(预登记后备结论生效):**YOLOE 嵌入可支撑"物体命名/朝向粗分",不能支撑
负空间地形判断;地形判断需另配深度/几何通道**(候选:单目深度头蒸馏、
时差视差、或直接读 MC 深度缓冲做训练时特权监督)。行为级绕沟导航按门控不启动。

### Y2b 归因分岔:DINO 也挂 → 通病非特例(terrain_probe_dino.py)

同协议同留出切分,唯一变量=特征源换 DINOv2-S/14 patch 网格(46×28):
**floor 0.473 / wall 0.836 / hole 0.353 → FAIL**(近洞 0.364/远洞 0.179)。
DINO 略好于 YOLOE(0.353 vs 0.266)但远不过门——**负空间盲区是 RGB 单帧嵌入的
通病,不是检测式校准嵌入的特例;深度/几何通道从"候选"升为"必需"**。
工程注:引擎深度缓冲实测退化(llvmpipe 软件渲染下全图恒 1.0,requires_depth
两种 conversion 均无效),特权深度须走解析几何(GT 工厂同源)。

### Y2c 深度充分性 oracle 上界(terrain_probe_depth.py)

解析光线步进(P3 网格 80×48,占用格=GT 工厂同源,0.03s/帧)作特权通道。
判据同 hole ≥0.5。引擎深度缓冲不可用(见 Y2b 工程注),特权几何走解析。

| 臂 | floor/wall/hole | 裁决 |
|---|---|---|
| depthonly(log深度,cin=1) | 0.593/0.453/**0.156** | FAIL |
| embdepth(YOLOE⊕深度) | 0.483/0.797/**0.310** | FAIL |
| **heightonly(反投影高度 h=dir_y·t)** | 0.645/0.587/**0.807** | **PASS** |
| embheight(YOLOE⊕高度) | 0.454/0.808/**0.367** | FAIL(融合问题,见下) |

**深度臂双 FAIL 的几何解读(本身是发现)**:
- 原始深度不是负空间的判别特征:conv 头平移不变、无位置先验,"深"既可能是洞
  也可能是远处地板,局部窗口内两者值域重叠;
- **掠射角失效**:眼高 1.62 视线擦过沟壕近沿直接打在远内壁沿口下方,深度与
  沿后地板几乎连续——负空间在深度图上本来就近乎不可见;
- 推论:几何通道的正确形式=**反投影高度图**(命中点相对眼高,地板恒 -1.62/
  洞更低/墙更高,平移不变),且可部署:单目深度+已知内参反投影,无特权。
  高度臂即测此假设。

**高度臂裁决(07-08):heightonly 单通道 PASS hole 0.807**——比 512 维 RGB 嵌入
(0.266/0.353)高 3 倍,负空间在正确的几何坐标系里是容易题。因果链闭合:
盲区根因不是"缺深度信息",是**缺正确坐标系的几何表征**(高度=平移不变的
类别判别特征;深度不是)。
- embheight 反而 FAIL(0.367):1 几何通道混入 512 语义通道被淹没(特征尺度/
  优化问题)——**几何与语义须分支融合**(late fusion / 独立地形头),朴素拼接无效。
  快塔架构落点:地形头吃高度图支路,物体命名头吃 YOLOE 支路,各管各的。
- 部署阶梯(登记):Y2d = RGB(+自身俯仰,本体感受非特权)→ 高度图预测网,
  解析高度做训练监督(=深度蒸馏的高度形式),预测高度重跑 heightonly 协议,
  门仍 hole ≥0.5——过了地形判断才算可部署收口。

**Y2d 裁决(07-08,terrain_height_distill.py):FAIL,失败模式=洞盲复发**。
hole 0.072 / floor 0.525 / wall 0.518,高度 MAE 0.178(±1 尺度≈0.7 格)。
HeightNet(0.5M 从零)学会了"给定俯仰的平滑房间几何"(floor/wall 可用),但洞
在其 RGB 特征里同样不可见——预测=无洞光滑地板。定位:684 帧从零训不出
shape-from-shading/遮挡先验。预登记后备生效:**Y2e = 蒸馏现成单目深度模型**
(Depth Anything V2 metric small → 俯仰+内参反投影高度 → 同协议同门),
测的是"真实世界深度先验能否零样本迁移到 MC 负空间"。

## 4.9 M3-pre 地图模块预研:北锚定 + clipmap 分级(2026-07-08,二期设计定案)

背景:用户问"空间结构/地图记忆放哪塔"→ 判定分时间尺度拆(秒级=快塔递归态
[M1 已验]/路线级=慢塔文本[C1b 雏形]/度量图=外置模块,两塔都不进权重);
再问"能否设计通用、优雅、可微、2D/3D 统一、记忆准确的地图结构"→ 定案
**自我中心因子化特征场**:K-planes 因子化(2D=1 平面,3D=3 平面,维度=选平面
组合数)+ 等变写入 M_{t+1}=decay·T_a(M_t)+write(o_t) + 门控衰减;MC 特化=
2.5D 列特征图(=Y2 高度图沿时间的等变积分,感知输出与地图写入格式天然对齐)。

**M3-pre 探针结果**(net/fovea_twotower/ego_map.py + map_probe.py,合成世界
12 种子×9 检查点,只考已观测地标,见 experiments-index):
- **分级精度(空间中心凹)预登记 PASS**:clipmap 3 级嵌套环 75% 预算下
  近场 0.977 无损、覆盖半径 2×(32-64 桶 0.594,均匀格无覆盖);
- **旋转重采样误差∝半径**(弧长 r·Δθ):文献默认的逐步重采样自我中心图
  中场塌到 0.365;**北锚定**(地图恒对齐世界朝向,平移=整格 roll+亚格偏移
  寄存器,旋转推迟到读出)单测证写入路径零插值损耗。近场门差 0.018 如实
  FAIL(门登记错桶),中场 +0.49 为事后口径。
- 定型:**北锚定 + clipmap + 通道开放**;立项前置=里程计漂移曲线(未做)。

**绑定关系(用户直觉 07-08,判定成立)**:距离分级结构绑定快塔,与 YOLO 头
高度相关,三层论证:
1. **阻抗匹配恒等式**:针孔相机角分辨率恒定(Δθ≈70°/360px)→ 距离 r 处像素
   足迹 ∝ r;倍增环 clipmap 格径也 ∝ r → **格径/传感器足迹之比跨环恒定**
   (0.5/1/2 格 @ 半径 16/32/64,比值恒 ≈0.58)。倍增环是常角分辨率传感器的
   精确阻抗匹配——地图永远不存传感器给不出的精度。经验对齐:Y2 远洞 IoU
   0.028(距离即失明)、E1 远距铁假阳性,YOLO 感知质量剖面本身就是距离分级的。
2. **写入所有权+节拍**:地图 step=逐 tick 自运动记账(O(1) roll),必须跑在
   快塔 36Hz 节拍上;写入特征=TokenHead 输出(类 softmax+conf+面积测距)+
   地形支路高度图——全是快塔器官。通道草案:4 类+conf+高度+时新度=7 通道。
3. **读出分层=两塔分界**:细环(近场)=快塔反应式读出(局部裁剪当输入通道);
   粗环(远场)=池化摘要喂慢塔规划。**"绑定快塔"对写入+近读成立;远粗环是
   慢塔的阅览室**——地图模块=两塔的边界器官,外置,不进任何塔的权重。

### 慢塔读图定案:查询 API(2026-07-08)

三方案裁决:全图文本序列化=schema 组合爆炸(M1 教训放大版)否;软 token
投影=零 schema 但需训练/破热更,留二期(底牌:GT 工厂可免费量产
(地图,空间问题,GT答案) 三元组,E2 式注入路径现成);**定案=查询 API**——
schema 成本从"序列化一张图"塌缩到 4 个查询原语(由慢脑四职能直接决定:
survey/nearest/path_home + 库存查询已有)。配套铁律:**几何计算全留在
地图模块(确定性代码),慢塔只消费模板文本**,LLM 永不碰坐标算术。
已实现:MapQuery(net/fovea_twotower/ego_map.py)。M4(查询API vs 全图
倾倒 vs 无图,C1b 式装配)登记待做。

### M3-loc:地图反向自定位,无真值(2026-07-08,map_loc_probe.py)

命题(用户):快塔能否利用地图反推自身正确位置?机制:MC yaw=本体感受
精确(相机动作积分),**漂移只在平移**(碰撞/动量)→北锚定下自定位=纯平移
扫描匹配:观测小图 vs 存图窗口互相关,峰=修正量 ê,施加=虚拟 step(ê)。
里程计噪声模型:测量=真实×(1+N(0,0.1))+碰撞 p=0.06(真实清零里程计不知情)。

标定(种子0-9,机制参数网格)→留出确认(种子100-109,门先登记):
| 门 | 结果 | 裁决 |
|---|---|---|
| G-loc1 修正 ≤0.4×航位推算 | 1.53 vs 4.7(0.33×) | **PASS** |
| G-loc2 误差有界 ≤2.0 | 曲线 0.76→1.53(dead 2.4→4.7 无界) | **PASS** |
| G-query 复合 | 距离误差0.8✓/survey查全1.0✓/方位0.62<0.8 | FAIL(方位分量) |

- 标定教训:亚格抛物线精化有害(NCC 归一化随位移畸变 2.83>2.04);锚定
  封笔无效(2.17,e_hist 污染非主导);**量化是主导误差**——0.5格分辨率+每3步
  修正 1.19(标定)/1.53(留出)。
- 方位分量失败待归因(疑=最近实例歧义+八分区边界量化,登记不改判)。
- 恒等式入档:**回家向量=-p̂,其误差≡定位误差**——"记回家路线"的质量
  直接由自定位质量决定,两职能同一机制。

## 5. 未决闸门(先于结果登记,不许改判)
- E1v2:追踪 ≤8° 且比 frozen 降 ≥40%;切换 ≥0.7;到达 ≥0.6 且比 random +0.15。
- E2:M2a(见过目标×新库存)≥0.8;M2b(留出目标)≥0.6。
- B0.5(播种独立价值):裸动作历史若追平 keys 优势 → 摘除播种通道。
- 全回路:指令链任务端到端成功率(判据待闭环原型后登记)。

## 6. 正式跑最终模型选项(2026-07-08 定案,F1 终审后)

| 组件 | 选项 | 依据 |
|---|---|---|
| 快头 | **22M(d512 L7)**,v17 配方(14k步+early-stop——C4证晚期过拟合) | F1 预登记规则裁决(CI平局→便宜臂);64M历史优势=小样本假象 |
| 前端 | YOLOE 校准 token(g1_vectors+conv_head_v4)+TokenHead 连通域 | Y1 双门PASS(6.3°/lock1.00 碾压 DINO 两臂) |
| 地图 | EgoMapNorthLoc 北锚定+clipmap 3级;**自定位默认关,按不确定度门控开** | M3-pre/M3-loc 预登记PASS;A1 真实MC:回家11/11成立,低漂移域修正有害(感知噪声>漂移),查询需实例聚类去重(0.27严/0.64宽) |
| 慢塔 | Nemotron-4B(冻结+LoRA),MapQuery 文本接口(nearest/survey) | Step4/W系;几何计算铁律留模块 |
| 动作头 | 回归+bin(现行);扩散头=引入人类多模态数据时再切 | 教师确定性→回归充分 |
| 数据 | trackcmd_v13+dagger(83局)+QC;**首要杠杆=数据扩容**(C3 引擎5万局/天) | 数据受限区征兆:>22M 容量无可测收益@83局 |
| 残留 | 切换缺口(0.35)→GRPO 收口;地形/软token/M4=二期 | C2/E1 登记 |

## 7. 规模外推与训练服务器配置(2026-07-08,runs/scaling_fit.json)

### 7.1 快头容量幂律(F2)

模型 err(N)=a·N^(-b)+c,**c 固定=12°(观测一致教师追踪误差=BC 监督天花板,
非自由参数)**;拟合容量受限区三点(2.4/8.8/22M,同数据 83 局):
a=15.85,b=0.353,rmse≈0.3°。64M 点(20.6°,F1 n=27)**排除在幂律外**——
n 加倍后反转=数据受限区,它本身就是"甜点已过"的直接证据。

| 外推(数据充分假设) | 22M | 64M | 128M | 512M |
|---|---|---|---|---|
| err 预测 | 15.0° | 13.7° | 13.1° | 12.5° |

**甜点=两线索交叉**:①实测:容量曲线 22M 处最后一次可测改善,64M 反转
→ 甜点 ≤22M@83 局;②外推:边际改善 <1°/倍容量(运行方差 3° 的 1/3,不可测)
处 N*≈28M。**结论:22M 就是当前配方的甜点,与 F1 裁决独立一致。**
关键量:22M 距教师天花板仅 2.2°——**容量杠杆全部剩余价值 ≤2°;
主杠杆=移动天花板本身(GRPO/更好教师/课程多样化)+数据扩容**(83 局
处于重度过参数化,C4 已证晚期过拟合)。数据侧指数待测(demo 曲线 24/48/96
点噪声太大无法拟合,登记:正式跑首件事=每技能 500/1000/2000 局三点曲线)。

### 7.2 算力需求核算(全部实测锚定)

| 工作负载 | 公式/实测 | 结果 |
|---|---|---|
| 22M BC 一轮 | 6·N·D=6·22e6·14k步·1024tok ≈1.9 PFLOP | 3090 实测 **2.5 分钟** |
| BC@50×数据 | 步数~10×(early-stop 决定) | **~25 分钟,仍可忽略** |
| 采样 | 536 局/时/环境(C3 稳态),线性到 N=4 复测 | 环境数×536/时 |
| 数据扩容目标 | 10 技能×2000 局=2 万局 | N=16 → **2.3 小时** |
| GRPO(切换收口) | 群 8×提示 16×迭代 300≈3.8 万局 | N=16→18h,**N=32→9h** |
| 慢塔 4B LoRA(引擎A) | 2k 样本×~1k tok,LoRA | 24G 单卡数小时 |
| 感知校准(引擎B) | 25 局/类+conv 头 3k 步 | 分钟级 |

**结构性结论:本工作负载不是 GPU 受限的,是环境吞吐受限的。**
瓶颈资源=CPU 核(llvmpipe 软渲染,~2-3 核/环境)+RAM(2.5G/环境:2G JVM+渲染);
GPU 需求=①批式 token 推理服务(YOLOE embed 批 16 帧 ~40ms → 一张 24G 卡
喂 16-32 环境@20Hz)②训练(22M 分钟级/4B LoRA 单卡)。

### 7.3 服务器配置推荐

**甜点单机(正式跑一期,~全部闸门工作负载)**:
- CPU 64 核(EPYC/Threadripper)——N=16-24 环境并行(2.5-3 核/环境含 JVM);
- RAM 128-256G——24 环境×2.5G=60G+推理/训练余量(本 30G 盒 8 环境已顶);
- GPU 2×24G(4090/L40S 级):一张常驻批式推理服务(采样/评测/GRPO rollout),
  一张训练+慢塔 LoRA;22M 训练用不满一张卡,双卡是为**采样-训练不互踩**;
- NVMe 2T(50k 局/天×~1.5MB/局 ≈75G/天)。
- 明确不推荐:8×A100/H100 节点——GPU 会闲置 >90%,钱买错了瓶颈。

**扩展路径(二期/集群)**:采样农场=纯 CPU 节点水平扩(llvmpipe 零 GPU,
已验),每 64 核节点 +16-24 环境;GPU 节点一台即可,直到慢塔全参微调
或视频世界模型进场才需要 A100 级——那是另一份预算申请,用它自己的曲线说话。

### 6.1 模型选项细节定案(2026-07-08 补全;可执行级配置)

**快头(执行塔,22M)**
- 结构:TrackNavTower,d=512/layers=7/heads=4/dropout=0.1/max_len=128;
  输入=goal 相对 token 折叠 [K=8, 8维](类无关结构)+上一步动作 22 维
  (dx,dy 归一 + 20 键);输出=相机 mu-law 11 bins×2(yaw/pitch,
  CAM_NORM_PX=120,±18°/步封顶)+ 20 键 sigmoid(阈 0.5)。
- 训练:bs16×seq64,lr 3e-4,14k 步基准;**early-stop 按留出 cam_acc**
  (C4:83 局时 ~1.5k 步即峰,数据扩容后峰后移);prev_dropout 0.5
  (因果混淆解药)+switch_os 0.5(切换窗口过采样);种子固定 0。
- 基准 ckpt:runs/trackcmd_bc_v17/best.pt;数据扩容后以 dcurve 系列重训替换。

**前端(感知,冻结骨干+小头)**
- UnifiedYoloe26 骨干**永冻结**;向量 bank=runs/g1_vectors.pt(域内 GT 嵌入
  判别拟合,非零样本 prompt);分割=ConvSegHead(cin=384) runs/g1_conv_head_v4.pt
  (感知 DAgger 版:闭环访问分布加训,教师锁定率 0.28→0.56 五连升的产物)。
- TokenHead:640×360→pad384 letterbox;conv 分割 argmax→连通域,min_area=150,
  K=8,token=[质心 xy+box+面积+类概率 C+1];右下角(>0.70w,>0.58h)屏蔽
  (第一人称手臂区,C1b"追自己的手"教训)。pf 提案通道只留开放集点名,
  不承担核心类(v6 假阳性教训)。
- 新类接入流程(引擎 B):文本 PE 冷启动→采域内格嵌入→逻辑回归级学习向量
  →需精掩膜再训该类 conv 通道;分钟级,骨干不动。

**地图(外置模块,两塔边界器官)**
- 生产形态=EgoMapClip 北锚定 3 级嵌套环(0.5/1/2 格分辨率,75% 预算
  2× 半径);通道草案 7=4 类 conf+高度+时新度+占据计数;
  写入=TokenHead 检测(方位:标定斜率 ≈0.151°/px+yaw;距离:k/√面积,
  k 标定;>10 格不写);step=整格 roll+亚格寄存器,O(1) 跑逐 tick。
- 自定位(_RelocMixin):互相关,window=6/min_ratio=1.3/min_pts=3/阻尼 0.35,
  **默认关**;开启条件=不确定度门控(自上次强锚定累计行走距离>阈,
  阈由漂移率标定——A1 实测追踪课程 220 步漂移仅 ~0.4-2.6 格,
  感知定位噪声 3-5 格,低漂移开修正必伤)。
- 慢塔接口=MapQuery:nearest(类)/survey(8 扇区)/path_home(=-p̂),
  输出模板中文文本;**铁律:坐标算术零进 LLM**。
  待修(登记):最近实例聚类去重+top-k(严口径 0.27→修复目标 ≥0.6)。

**慢塔(规划,4B)**
- Nemotron-4B 冻结+LoRA(qkvo);think 模式 SFT(留出 think 率 1.00);
  知识注入=引擎 A 规则引擎(42 目标×2000 样本档),**程序进权重、
  事实走上下文**(配方书随版本热更,E2c:分布内 1.00/未见目标带卡 0.73);
  递归状态不承担视觉记忆(W4 三连败+谱测量定位)。
- 运行时:低频事件触发;输出→goal 向量(O(1) 插拔)+感知点名+地图查询;
  两次输出间 goal 零阶保持。

**教师/数据引擎(引擎 C)**
- 教师=TokenTeacher:**观测一致**(只消费学生同款 token)+**确定性**
  (恒右转搜索,潜变量可观测铁律)+贴脸后退,停靠阈 AREA_NEAR=0.038;
- 采集协议:开局扰乱(yaw±40/pitch-15~10)+局中切换 every 40±10 步
  +DAgger β=0.15;QC=schema+教师段末≤25°(v18 批拒收 37%);
- 相机口径:CAM_NORM_PX=120(±18°/步),勿用人类小步 10px 口径。

**动作头**:回归+bin argmax(确定性教师→条件均值无多模态坍塌);
扩散头=引入人类/VPT 多模态示范时的既定切换点,非现在。

**评测协议(制度化)**:eval 种子 11 起,220 步/110 切换,n≥16,
episode 级 bootstrap 95%CI(B=1e4);三门 T1≤8°/T2 切换≥0.7/T3 到达≥0.6
+学生/教师相对口径并报;比例类差距 <0.1 视为噪声(C4 标尺)。

**残留缺口(全部登记有主)**:切换 0.35→GRPO(唯一正式跑拦路项);
查询实例聚类(A1);地形/软 token/M4/远行课程自定位=二期。
