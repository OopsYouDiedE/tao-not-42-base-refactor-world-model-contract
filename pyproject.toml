[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tao-not-42"
version = "0.2.0"
description = "Godot 元学习强化学习训练环境"
readme = "readme.md"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = [
    "numpy",
    "gymnasium",
    "opencv-python",
    "stable-baselines3",
    "torch",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
train-godot-ppo = "train.godot_meta_rl.train_ppo:main"
train-minecraft-bc = "train.minecraft.bc_warmstart:main"

[tool.setuptools.packages.find]
include = ["blocks*", "net*", "train*", "utils*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
addopts = "-q --tb=short"

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.isort]
profile = "black"
line_length = 100
known_first_party = ["train", "utils"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
